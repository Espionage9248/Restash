[project]
name = "restash"
version = "0.1.0"
description = "Freshness scoring for Stash scenes and performers (custom_fields)."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"
