# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Restash local state / secrets (never commit)
restash_state.json
.restash.local.json

# Built plugin-source site (generated by scripts/build_source.sh / pages.yml)
_site/

# Project docs — kept local only, not tracked (per user preference)
docs/
